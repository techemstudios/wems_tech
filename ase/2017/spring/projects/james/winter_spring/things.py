class Dog(mamals):
	"""a simpt attempt to model a dog"""
	
	def _init_(self, name,age):
"""Inialize name and attribut."""

		self.name = name
		self.age = age
		
	def sit(self):
		"""Simulate a dag sitting in response to a cammand."""
		pint(self.name.title() + " is now sitting.")
		
		def rol_over(self):
			"""Slmulate rolling over ln reponse to a command."""
			print(self.name.title() + " rolled over!")
		

	
	


	
		
	
	i
